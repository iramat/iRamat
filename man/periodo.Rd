% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/periodo.R
\name{periodo}
\alias{periodo}
\title{Create a timeline of periods from a PeriodO authority}
\usage{
periodo(
  periodo_authority = "https://n2t.net/ark:/99152/p02chr4",
  use_periodo = FALSE,
  min_date = NA,
  max_date = NA,
  time_match = NA,
  seriated = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{periodo_authority}{A string. URL of a PeriodO authority
(default: "https://n2t.net/ark:/99152/p02chr4", INRAP).}

\item{use_periodo}{Logical, if TRUE, the function is used as a subroutine
(typically called from [chrono()]) and the plot title is suppressed.}

\item{min_date}{Optional numeric. Lower bound of the time interval (most ancient date).
Default: `NA` (no lower bound).}

\item{max_date}{Optional numeric. Upper bound of the time interval (most recent date).
Default: `NA` (no upper bound).}

\item{time_match}{Numeric between 0 and 1. Minimum proportion of temporal overlap
required between a period and the given [min_date, max_date] interval
for the period to be included. Default: `NA` (no filtering).}

\item{seriated}{Logical, if TRUE (default), reorder periods by earliest start date.}

\item{verbose}{Logical, if TRUE (default), print progress messages.}
}
\value{
A list with one ggplot2 object:
  \describe{
    \item{periodo}{A timeline of PeriodO periods.}
  }
}
\description{
This function reads a PeriodO authority file (CSV + JSON metadata) and
returns a ggplot2 timeline of the defined periods. Optionally, the timeline
can be restricted to a given temporal range, and periods can be filtered by
the amount of overlap with that range.
}
\examples{
\dontrun{
# Default authority (INRAP)
periodo()

# Restrict to a specific interval and require exact overlap
periodo(min_date = -700, max_date = 0, use_periodo = TRUE, time_match = 1)
}

}
